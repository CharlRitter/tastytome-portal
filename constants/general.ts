export const DRAWER_WIDTH = 240;

export enum StatusTypes {
  Pending = 'Pending',
  Fulfilled = 'Fulfilled',
  Rejected = 'Rejected'
}

export enum Mode {
  Add = 'add',
  Edit = 'edit',
  View = 'view'
}

export enum ThemeStates {
  System = 1,
  Dark = 2,
  Light = 3
}
